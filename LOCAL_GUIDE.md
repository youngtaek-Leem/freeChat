# 🚀 Local AI Development Guide

이 문서는 이 프로젝트에서 로컬 인공지능 모델을 활용하는 방법과 규칙을 정의합니다.

## 🔗 로컬 서버 설정
- **Base URL**: `http://127.0.0.1:11434/v1`
- **사용 중인 모델**: `gemma4:12b` (Ollama Gemma 4 12B)
- **제공 방식**: OpenAI 호환 API (`/v1/chat/completions`)

## 🤖 `@local` 워크플로우
대화 중에 `@local` 키워드를 사용하면 다음과 같은 규칙에 따라 개발이 진행됩니다.

1.  **로컬 우선 순위**: 프롬프트 앞에 `@local`이 붙어 있으면, AI 어시스턴트는 클라우드 모델 대신 지정된 로컬 서버에 질의를 먼저 수행합니다.
2.  **데이터 보안**: 민감한 로컬 환경 코드나 데이터는 로컬 모델 내에서 처리하여 외부 유출을 최소화합니다.
3.  **코드 생성 및 적용**: 로컬 모델이 제안한 코드 스니펫이나 로직을 기반으로 프로젝트 파일을 수정하거나 새로운 기능을 구현합니다.

### 💡 사용 예시
> **User**: `@local` 현재 `index.html`에 다크모드 전환 버튼 기능을 추가해줘.

위와 같이 요청하면, 어시스턴트는 로컬 Gemma 4 모델에게 구현 방법을 물어보고 그 결과로 개발을 수행합니다.

---
*이 가이드는 `antigravity.config.json` 설정과 연동되어 관리됩니다.*
